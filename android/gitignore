*.iml
.gradle
/local.properties
/.idea/workspace.xml
/.idea/libraries
.DS_Store
/build
/captures
.cxx
local.properties
# The binary gradle-wrapper.jar is generated by the CI workflow.
# Do not commit it — the workflow runs `flutter create --platforms android`
# which regenerates it from scratch every build.
gradle/wrapper/gradle-wrapper.jar
